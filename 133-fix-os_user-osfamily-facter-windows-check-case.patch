diff --git a/modules/pt_config/lib/puppet/type/shared/os_user.rb b/modules/pt_config/lib/puppet/type/shared/os_user.rb
index aa6db98..9c1074c 100644
--- a/modules/pt_config/lib/puppet/type/shared/os_user.rb
+++ b/modules/pt_config/lib/puppet/type/shared/os_user.rb
@@ -23,7 +23,7 @@ newparam(:os_user) do
   desc "The OS user of the domain."
 
   validate do |value|
-    if Facter.value(:osfamily) == 'Windows'
+    if Facter.value(:osfamily) == 'windows'  # <bloodmc dpkpatch/>
       fail("Unable to execute commands as other users on Windows")
     elsif !Puppet.features.root? && Etc.getpwuid(Process.uid).name != value
       fail("Only root can execute commands as other users")
